import { Job_advertisement } from './job-advertisement.model';

describe('Job_advertisement', () => {
  it('should create an instance', () => {
    expect(new Job_advertisement()).toBeTruthy();
  });
});
